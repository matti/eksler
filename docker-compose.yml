version: '3'

services:
  eksler:
    build: .
    image: mattipaksula/eksler
    entrypoint:
      - "/app/entrypoint.sh"
    volumes:
      - .:/app
      - ./.bash_history:/root/.bash_history
      - $HOME/.kube:/root/.kube
    env_file:
      - .env
